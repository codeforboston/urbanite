<div class="home">
  <h1><%= @title %></h1>
  <p><%= @intro_mssg %></p>
  <div ng-controller="EventsCtrl">
    <div class="event-list-container" >
      <ul>
        <li ng-repeat="event in events">
          <h3>{{event.name}} <small>({{event.description}})</small></h3>
        </li>
      </ul>
    </div>
  </div>
</div>